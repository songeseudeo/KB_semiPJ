package com.kb.realestate.repository;

import com.kb.realestate.entity.ChecklistList;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChecklistListRepository extends JpaRepository<ChecklistList, Long> {
}